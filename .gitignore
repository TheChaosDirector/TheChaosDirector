__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Secrets — never commit API keys
.env
.env.*
!.env.example

# Data lake and run artifacts are local-only (can be rebuilt with the CLI)
data/
artifacts/
*.zip
