__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Secrets — never commit API keys
.env
.env.*
!.env.example

# Data lake and run artifacts are local-only (can be rebuilt with the CLI)
data/
artifacts/
*.zip

# Daily choice diary IS meant to be committed (future training data).
# Keep the folder even before the first live day.
!experience/
!experience/**
experience/**/*.parquet
experience/**/*.tmp
